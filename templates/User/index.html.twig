{% extends "base.html.twig" %}
{% block body %}
    <h1>Members</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.id }}</li>
            <li>{{ user.nom }}</li>
            <li>{{ user.prenom }}</li>
            <li>{{ user.email }}</li>
            <li>{{ user.github }}</li>
            <li>{{ user.discord }}</li>
        {% endfor %}
    </ul>
{% endblock %}